@model Post
@{
    ViewBag.Title = "Post";
}

<h2>@Model.Title</h2>

@if (!String.IsNullOrEmpty(Model.Image))
{
    var image_path = $"/Image/{Model.Image}";
    <img src="@image_path" width="300" />
}
<div class="post-body">
    @Html.Raw(Model.Body)
</div>


